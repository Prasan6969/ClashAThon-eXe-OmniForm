# Architecture
The application is structured using a modern SaaS architecture. The architecture is
shown as below.

Frontend (React SPA)

↓

HTTPS REST API (JSON + Bearer Token)

↓

Backend (Node.js + Express)

↓

Database (MongoDB Atlas) + Media Storage (ImageKit)